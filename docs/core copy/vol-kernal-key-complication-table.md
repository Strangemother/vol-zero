The table definition allows for the _recompilation_ of the kernel.

Initially the primary execution table is collect from the first draft of the memory table install; This is done through a new memory table and its automated loadout.

