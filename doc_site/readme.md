# Documentation Site

This directory contains the source and assets for the project's custom documentation site.

## Getting Started

It's a Flask-based web application that serves the project's documentation.

```bash
pip install -r doc_site/requirements.txt
python doc_site/app.py
```

This will serve the app, typically accessible at `http://127.0.0.1:9050/` in your web browser.