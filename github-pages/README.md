# VOL Zero GitHub Pages

This folder contains the static GitHub Pages site for VOL Zero.

The page is intentionally plain HTML and CSS. GitHub Pages deploys it through
the workflow in `.github/workflows/github-pages.yml`, which uploads this folder
as the Pages artifact.

## Local preview

Open `index.html` in a browser, or serve the folder with any static file server.
